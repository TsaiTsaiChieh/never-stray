export enum Ternary {
  TRUE = 'T',
  FALSE = 'F',
  UNKNOWN = 'U',
}

export enum PetRef {
  GOV = 'gov',
  MAP = 'map',
  OWN = 'own',
}

export enum PetSex {
  ALL = 'ALL',
  FEMALE = 'F',
  MALE = 'M',
  UNKNOWN = 'U',
}

export enum PetAge {
  ALL = 'All',
  ADULT = 'A',
  CHILD = 'C',
}

export enum PetStatus {
  UNKNOWN = 'unknown',
  OPEN = 'open',
  ADOPTED = 'adopted',
  OTHER = 'other',
  DEAD = 'dead',
}

export enum PetKind {
  ALL = 'all',
  CAT = 'cat',
  DOG = 'dog',
}
