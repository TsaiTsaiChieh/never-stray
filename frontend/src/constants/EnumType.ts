export enum Ternary {
  TRUE = 'T',
  FALSE = 'F',
  UNKNOWN = 'U',
}

export enum PetRef {
  GOV = 'gov',
  MAP = 'map',
}

export enum PetSex {
  FEMALE = 'F',
  MALE = 'M',
  UNKNOWN = 'U',
}

export enum PetAge {
  ADULT = 'A',
  CHILD = 'C',
  UNKNOWN = 'U',
}

export enum PetStatus {
  OPEN = 'open',
  UNKNOWN = 'unknown',
  ADOPTED = 'adopted',
}

export enum PetKind {
  ALL = 'all',
  CAT = 'cat',
  DOG = 'dog',
}
