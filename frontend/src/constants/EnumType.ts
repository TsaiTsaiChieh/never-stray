export enum Ternary {
  TRUE = 'T',
  FALSE = 'F',
  UNKNOWN = 'U',
}

export enum PetRef {
  GOV = 'gov',
  MAP = 'map',
  OWN = 'own',
}

export enum PetSex {
  FEMALE = 'F',
  MALE = 'M',
  UNKNOWN = 'U',
}

export enum PetAge {
  ADULT = 'A',
  CHILD = 'C',
  UNKNOWN = 'U',
}

export enum PetStatus {
  UNKNOWN = 'unknown',
  OPEN = 'open',
  ADOPTED = 'adopted',
  OTHER = 'other',
  DEAD = 'dead',
}

export enum PetKind {
  ALL = 'all',
  CAT = 'cat',
  DOG = 'dog',
}
